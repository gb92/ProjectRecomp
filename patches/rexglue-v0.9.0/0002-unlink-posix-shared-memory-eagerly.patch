diff --git a/src/core/memory_posix.cpp b/src/core/memory_posix.cpp
index fce701e..1cedfa2 100644
--- a/src/core/memory_posix.cpp
+++ b/src/core/memory_posix.cpp
@@ -377,6 +377,9 @@ FileMappingHandle CreateFileMappingHandle(const std::filesystem::path& path, siz
     shm_unlink(full_path.c_str());
     return kFileMappingHandleInvalid;
   }
+  // The descriptor and mappings keep the object alive. Removing its name now
+  // ensures the kernel reclaims it if the process exits without cleanup.
+  shm_unlink(full_path.c_str());
   return static_cast<FileMappingHandle>(ret);
 #endif
 }
