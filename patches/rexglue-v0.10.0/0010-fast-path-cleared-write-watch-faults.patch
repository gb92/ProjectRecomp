diff --git a/src/system/xmemory.cpp b/src/system/xmemory.cpp
index 76cc877..6b53850 100644
--- a/src/system/xmemory.cpp
+++ b/src/system/xmemory.cpp
@@ -561,9 +561,10 @@ bool Memory::AccessViolationCallback(std::unique_lock<std::recursive_mutex> glob
     return true;
   }
 
-  // Recovery path for stale host protection state in physical memory:
-  // if guest metadata says the page is writable but host protection is still
-  // read-only / no-access, restore write access and resume execution.
+  // Another thread may have cleared this watch after the fault was queued but
+  // before this thread acquired the global lock. If guest metadata still marks
+  // the page writable, TriggerCallbacks finding no watch means the first thread
+  // has already restored host write access.
   if (is_write) {
     constexpr uint32_t kWriteProtectMask =
         memory::kMemoryProtectWrite | memory::kMemoryProtectWriteCombine;
@@ -604,17 +605,7 @@ bool Memory::AccessViolationCallback(std::unique_lock<std::recursive_mutex> glob
       }
     }
     if (allow_write) {
-      size_t host_page_size = rex::memory::page_size();
-      uintptr_t page_base =
-          reinterpret_cast<uintptr_t>(host_address) & ~(uintptr_t(host_page_size - 1));
-      if (rex::memory::Protect(reinterpret_cast<void*>(page_base), host_page_size,
-                               rex::memory::PageAccess::kReadWrite, nullptr)) {
-        REXSYS_WARN(
-            "Recovered stale physical page protection for guest {:08X} (host {:016X}, "
-            "guest_protect {:08X})",
-            virtual_address, static_cast<uint64_t>(page_base), guest_protect);
-        return true;
-      }
+      return true;
     }
 
     uint32_t current_guest_protect = 0;
